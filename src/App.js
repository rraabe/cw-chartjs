import React from "react";
import BarChart from "./BarChart";
import './App.css';

const styles = {
  fontFamily: "sans-serif",
  textAlign: "center"
};

const App = () => (
  <div style={styles}>
    <BarChart />
  </div>
);

export default App;
